# CML model image only (``cdsw-build.sh`` default). Keeps the registry push small.
# Training, Streamlit, and LSTM/MiniLM RAG: use ``requirements.txt`` in Workbench or Jobs.
#
# Behavior: ARIMA + GBM + sparse GBM; RAG with TF-IDF index; ``lstm_next_step`` may be null.

pandas>=2.1.0
numpy>=1.24.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
joblib>=1.3.0
requests>=2.31.0
pypdf>=4.0.0
